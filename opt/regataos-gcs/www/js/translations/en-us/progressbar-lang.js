// English language translation
$(document).ready(function() {
	// Full progress bar
	$(".down-speed-desc").text("Speed:");
	$(".eta-desc").text("remaining.");
	$(".more-info").attr({title:"More information"});
	$(".pause").attr({title:"Pause download"});
	$(".play").attr({title:"Start download"});
	$(".cancel").attr({title:"Cancel"});
	$(".close-button").attr({title:"Remove"});
});

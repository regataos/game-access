// Portuguese language translation
$(document).ready(function() {
	// Full progress bar
	$(".down-speed-desc").text("Velocidade:");
	$(".eta-desc").text("restante(s).");
	$(".more-info").attr({title:"Mais informações"});
	$(".pause").attr({title:"Pausar o download"});
	$(".play").attr({title:"Iniciar o download"});
	$(".cancel").attr({title:"Cancelar"});
	$(".close-button").attr({title:"Remover"});
});
